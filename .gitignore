# Rust build artifacts
/target

# The benchmark crate is its own (excluded) workspace, so it builds into a
# separate target dir the root `/target` rule above doesn't cover.
/bench/spark-ecs-bench/target

# Backup files from rustfmt
**/*.rs.bk

# Perf data
perf.data
perf.data.old
flamegraph.svg

# IDE / editor
.vscode/
.idea/
*.iml
*.swp
*.swo

# macOS
.DS_Store

# Windows
Thumbs.db
ehthumbs.db

# Linux
*~
.directory

# Env files
.env
.env.local
